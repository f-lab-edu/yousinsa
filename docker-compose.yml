version: "3"

services:
  app:
    container_name: yousinsa-app
    image: yousinsa:0.0.1
    build:
      context: .
      dockerfile: ./app.Dockerfile
    ports:
      - "8081:8080"
    volumes:
      - ./app:/app
    working_dir: /app
    restart: always


